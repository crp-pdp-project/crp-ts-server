import { RelationshipDTO } from 'src/app/entities/dtos/service/relationship.dto';

export default {
  id: 0,
  name: 'Titular de la cuenta',
  isDependant: false,
} as const satisfies RelationshipDTO;
